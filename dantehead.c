#define stampa printf
#define leggi scanf
#define fopen apriFile
#define file FILE*
#define fscanf leggiFile
#define fprintf scriviFile
#include <stdio.h>
#include <stdlib.h>
#define libMatematica <math.h>
#define max(x, y) (((x) > (y)) ? (x) : (y))
#define min(x, y) (((x) < (y)) ? (x) : (y))
#define rad2 sqrt
#define eleva pow
